[project]
name = "patterns-not-solutions-demo"
version = "0.1.0"
description = "Demo code for 'Patterns are Not Solutions (And Never Were)'"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
